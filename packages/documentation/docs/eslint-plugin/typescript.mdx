import config from '@actinc/eslint-plugin/configs/typescript';
import CodeBlock from '@theme/CodeBlock';

The typescript configuration is used for generic typescript projects.

<CodeBlock
  language="json"
  title="ESLint Configuration"
  showLineNumbers>
  {JSON.stringify(config, null, 2)}
</CodeBlock>
