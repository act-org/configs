import config from '@actinc/eslint-plugin/configs/base';
import CodeBlock from '@theme/CodeBlock';

Base is the default starting ground for ESLint configurations.  All other configurations are layered on top of this.  It's ok to use this
for vanilla Javascript projects
<CodeBlock
  language="json"
  title="ESLint Configuration"
  showLineNumbers>
  {JSON.stringify(config, null, 2)}
</CodeBlock>
